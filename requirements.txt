# Route Resilience — aggregate local/CI environment.
# Torch is installed separately for the target CPU/CUDA runtime (see SETUP.md).

-r deploy/requirements-app.txt
-r requirements-train.txt
-r requirements-dev.txt
